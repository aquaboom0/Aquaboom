<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
  <!-- HTTP to LAN / Metro / emulator for local AquaRush API (see app.json expo.extra.apiLanHost). -->
  <base-config cleartextTrafficPermitted="true">
    <trust-anchors>
      <certificates src="system" />
    </trust-anchors>
  </base-config>
</network-security-config>
